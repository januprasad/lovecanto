<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jenuine.lovetips">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Indigo">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/IndigoWithoutActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoveMatch"
            android:label="@string/app_name"
            android:theme="@style/IndigoWithoutActionBar">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity
            android:name=".ListTitles"
            android:label="@string/app_name"
            android:theme="@style/IndigoWithoutActionBar"></activity>
        <activity
            android:name=".PostActivity"
            android:label="@string/app_name"
            android:theme="@style/IndigoWithoutActionBar">

        </activity>


        <activity
            android:name=".NextActivity"
            android:label="Next Screen"></activity>

        <receiver android:name=".MyReceiver" />
        <service android:name=".MyAlarmService"></service>

    </application>

</manifest>
